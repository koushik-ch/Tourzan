header.header
    nav.nav.nav--tours
        a.nav__el(href='/') All tours 
    .header__logo
        img(src='/img/tarzan-icon.png' alt='Tourzan logo')
    nav.nav.nav--user
        if user
            a.nav__el.nav__el--logout Logout
            a.nav__el(href='/me')
                img(src=`/img/users/${user.photo}` alt='User').nav__user-img
                span=user.name.split(' ')[0]

        else
            a(href='/login').nav__el Login
            a(href='/signup').nav__el.nav__el--cta Sign Up